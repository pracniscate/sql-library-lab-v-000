CREATE TABLE Series (
  id INTEGER PRIMARY KEY,
  author_id INTEGER,
  Sub-Genre INTEGER,
  title TEXT
);

CREATE TABLE Sub-Genre (
  id INTEGER PRIMARY KEY,
  name TEXT
);

CREATE TABLE authors (
  id INTEGER PRIMARY KEY,
  name TEXT
);

CREATE TABLE books (
  id INTEGER PRIMARY KEY,
  title =
);
